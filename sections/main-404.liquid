<section class="section">
  <div class="page-width page-hero">
    <h1>{{ '404.heading' | t }}</h1>
    <p class="lede">{{ '404.text' | t }}</p>
    <a class="btn btn--primary" href="{{ routes.root_url }}">{{ '404.cta' | t }}</a>
  </div>
</section>

{% schema %}
{
  "name": "t:sections.main_404.name",
  "settings": []
}
{% endschema %}
